services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: smart_pest
      POSTGRES_PASSWORD: smart_pest
      POSTGRES_DB: smart_pest
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
